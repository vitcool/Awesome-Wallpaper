<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/counter_text_color</item>
        <item name="colorPrimaryDark">@color/list_item_title</item>
        <item name="colorAccent">@color/grid_bg</item>
    </style>

</resources>
